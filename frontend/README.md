# frontend-static-demo

This is a tiny static project (HTML/CSS/JS) for testing RunParn frontend preview.

## Requirements (RunParn)
- `index.html` in repo root ✅

## What you'll see
- A simple page with a button that updates a text label.
